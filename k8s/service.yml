  apiVersion: v1
  kind: Service
  metadata:
    name: quingo-frontend-service
    namespace: quingo-app
  spec:
    selector:
      app: quingo-frontend
    ports:
    - port: 80      
      targetPort: 3000
    type: ClusterIP